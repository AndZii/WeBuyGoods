<%= form_for(@caurusel_image, html: {class: 'form-horizontal'}) do |f| %>
  <% if @caurusel_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caurusel_image.errors.count, "error") %> prohibited this caurusel_image from being saved:</h2>

      <ul>
      <% @caurusel_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name, class: 'col-sm-2 control-label' %><br>
    <div class="col-sm-10">
    <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="field form-group">
    <%= f.label :img, class: 'control-label col-sm-2' %><br>
    <div class="col-sm-10">
    <%= f.file_field :img, class: 'form-controll' %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
