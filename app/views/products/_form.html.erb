<%= form_for(@product, html: {class: 'form-horizontal'}) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %><br>
    <div class="col-sm-10">
    <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>
    <div class="field form-group">
                  <%= f.label :text, class: "col-sm-2 control-label" %><br>
                <div class="col-sm-10">
             <%= f.text_area :text, class: "form-control" %>
        </div>
  </div>

    <div class="field form-group">
      <%= f.label :price, class: "col-sm-2 control-label" %><br>
      <div class="col-sm-10">
        <%= f.text_field :price, class: "form-control" %>
      </div>
    </div>

    <div class="field form-group">
      <%= f.label :first_img, class: "col-sm-2 control-label" %><br>
      <div class="col-sm-10">
        <%= f.file_field :first_img, class: 'form-control' %>
      </div>
    </div>
    <div class="field form-group">
      <%= f.label :second_img, class: "col-sm-2 control-label" %><br>
      <div class="col-sm-10">
        <%= f.file_field :second_img, class: 'form-control' %>
      </div>
    </div>

  <div class="actions">
    <%= f.submit  class: 'btn btn-success'%>
  </div>
<% end %>
